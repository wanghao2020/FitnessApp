# 2026-06-12 Demo Seed

## 任务

实现一个 DEBUG demo seed，让 app 在没有真机、HealthKit、Watch 或本地模型文件时也能展示完整闭环。

## 执行清单

- [x] 增加 Core 测试，验证 `FitnessRPGDemoSeed.showcase` 覆盖 Today、History、Memory、Weekly Polish、Validation Archive。
- [x] 增加启动参数测试，验证 `--fitnessrpg-demo-seed` 解析。
- [x] 增加 Persistence 测试，验证 demo seed 可以一次性写入所有 JSON 集合。
- [x] 实现 `FitnessRPGDemoSeed`。
- [x] 实现 `JSONFitnessRPGStore.saveDemoSeed(_:)`。
- [x] 在 `TodayPersistenceModel` 增加 `applyDemoSeed(_:)`。
- [x] 在 `FitnessRPGApp` DEBUG 启动流程接入 `--fitnessrpg-demo-seed`。
- [x] 更新 README / native README。
- [x] 运行 Swift Package 测试、iOS/watchOS 构建验证，并在 iPhone 17 模拟器用 demo 参数启动检查。
